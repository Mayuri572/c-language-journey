#include <stdio.h>

// Demonstrating use of some C keywords

int main() {
    int number = 10;

    if (number > 0) {
        printf("Number is positive\n");
    }

    return 0;
}

// Output:
// Number is positive